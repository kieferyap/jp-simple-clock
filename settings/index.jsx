function clockSettings(props) {
  return (
    <Page>
      <Section title="Tap the date in your Fitbit to change its format.">
      </Section>
      <Section title="Tap the time in your Fitbit to change its format.">
      </Section>
    </Page>
  );
}

registerSettingsPage(clockSettings);